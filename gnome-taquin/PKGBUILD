# $Id: PKGBUILD 264971 2016-04-14 21:08:51Z heftig $
# Maintainer: Jan Alexander Steffens (heftig) <jan.steffens@gmail.com>
# Contributor: Jan de Groot <jgc@archlinux.org>

pkgname=gnome-taquin
pkgver=3.21.91
pkgrel=1
pkgdesc="Move tiles so that they reach their places"
url="https://wiki.gnome.org/Apps/Taquin"
arch=(x86_64 i686)
license=(GPL3)
depends=(gtk3 librsvg libcanberra)
makedepends=(intltool vala itstool)
groups=(gnome-extra)
install=gnome-taquin.install
options=(!emptydirs)
source=(https://download.gnome.org/sources/$pkgname/${pkgver:0:4}/$pkgname-$pkgver.tar.xz)
sha256sums=('87e826ea0a0b01160c04ef651cef6491fa764ea2f2fb12c666e8f8e07a5fdec7')

build() {
  cd $pkgname-$pkgver
  ./configure --prefix=/usr --sysconfdir=/etc --localstatedir=/var
  make
}

package() {
  cd $pkgname-$pkgver
  make DESTDIR="$pkgdir" install
}
